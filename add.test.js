const { default: ListCollection } = require('./modules/ListCollection');

const data = new ListCollection();
test ('Test the add item function to To-Do List', () => {
    expect(data.setLocalData()).toBe();
});